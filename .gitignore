# Build artifacts
/target/
/dist/
# The WASM crate is excluded from the workspace, so it builds to its own target/.
crates/atlas-wasm/target/
# wasm-pack output for the web app (built in CI / locally via `fledge build web`).
web/app/pkg/
# self-atlas badges + SVG components, rendered into the Pages artifact in CI.
web/app/badges/

# OS
.DS_Store
Thumbs.db

# Local agent worktrees and session state
.claude/

# generated self-atlas demo (screenshots in docs/ are committed instead)
atlas.html
*.atlas.html

# coverage + CI scratch
lcov.info
atlas.json
